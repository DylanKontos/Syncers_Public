using UnityEditor;
using UnityEngine;

public class EdgegapManager : MonoBehaviour // IDENTIFIER CLASS IDENTIFIER CLASS IDENTIFIER CLASS IDENTIFIER CLASS 
{

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
